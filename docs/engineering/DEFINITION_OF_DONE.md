# Definition of Done

A task is complete only when:

- Implementation matches the relevant spec.
- Tests are added or updated.
- Docker/local run commands are documented.
- Security checklist is reviewed.
- No session/cookie data is logged.
- README or docs are updated if behavior changes.
- Known limitations are documented.

For downloader changes, include sample URLs used for manual verification, but do not commit private URLs or credentials.
